/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  images: {
    domains: ['www.pandasecurity.com', 'assets.materialup.com'],
  },
};

module.exports = nextConfig;
